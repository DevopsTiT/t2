##############################################################################
# Provider stubs for documentation / IDE. Real apply uses environments/*/ .
# Copy this pattern into each environment root (already done under environments/).
##############################################################################

# provider "dynatrace" {
#   dt_env_url   = var.dt_env_url
#   dt_api_token = var.dt_api_token
# }
#
# Optional: AWS Secrets Manager for tokens (see environments/*/secrets.tf.example)
# provider "aws" {
#   region = var.aws_region
# }
